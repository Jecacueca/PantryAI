<h1>Pantry show</h1>
<%= link_to "+ Item", new_item_path %>
 <p><%= @pantry.items.count %> items</p>
<ul>
  <% @pantry.items.each do |item| %>
    <%= render "shared/item_card", item: item %>
  <% end %>
</ul>
